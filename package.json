{
  "name": "hofi",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build index.ts --target=bun --minify --outfile dist/hofi.js",
    "compile": "bun build index.ts --compile --minify --outfile ./bin/hofi",
    "hofi": "./bin/hofi"
  },
  "bin": "dist/hofi.js",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cac": "^7.0.0",
    "chalk": "^5.6.2",
    "zod": "^4.4.3"
  }
}
